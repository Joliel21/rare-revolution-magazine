# RARE Employment

- `articles.json` contains 4 article records.
- `source/rare-employment.csv` preserves the uploaded source CSV.
- Article images currently use the source CSV's external Microlink image URLs; no local image mapping was invented.
- Author values remain `null` because the source CSV does not provide authors.
- This series is staged separately and is not currently one of the 15 RARE INSIGHTS manifest entries.
